# Copilot Instructions

## C# Naming
- Compile-time constants must use SCREAMING_CASE, including static readonly values such as TimeSpan fields.
- Treat `const` fields and `static readonly` values that act as compile-time constants the same for naming purposes.
- Example: use `COMPILE_TIMEOUT`, not `CompileTimeout`.
