
module Zurfur


use Zurfur.Collections[Iterator]
use Zurfur[Clone]
use Zurfur.Math[max]

// Use this for now until require is implemented
fun require_tbd(c bool) require c {}


[pub] const MAX_ARRAY_SIZE_BYTES int = i32.MaxValue/4

// UNSAFE: This type may contains ?T and relies on List to keep track of
// how many items it has.
type unsafe ListBuffer<T> is Buffer<T>

// TBD: Protect the list against being moved while there is a reference to an item
[pub] fun get ListBuffer<T>.isLocked() bool
    todo

// TBD: Implement "type is" instead of stubbing like this
fun get ListBuffer<T>.count() int {}

[pub] type List<T>
    [pub] const MaxItems int = MAX_ARRAY_SIZE_BYTES/Math.Max(1,sizeof(t))
    buffer ListBuffer<T>
    count int pub ro
    version int pub ro

[pub] fun List<T>.isLocked() bool
    return my.buffer.isLocked

[pub] fun mut List<T>.incVersion()
    my.version += 1
    

[pub] fun List<T>.new(capacity int)
    my.buffer = ListBuffer<T>(capacity)

// Clone items into the new list
[pub] fun List<T>.new(items []T)
    my.buffer = ListBuffer<T>(items.Count)
    my.push(items)

// Clone items into the new list
[pub] fun List<T>.new(items mut Iterator<T>)
    my.buffer = ListBuffer<T>(items.countHint)
    my.push(items)
    
[pub] fun _opIndex<T>(list List<T>, i int) mut ref T
    require i in 0..count
    return ref list.buffer[i]

[pub] fun _opIndex<T>(list List<T>, r Range<int>) mut []T
    require r.start in 0..count
    require r.end in 0..count
    require r.start <= range.end
    return ref list.buffer[r]



// Grow the capacity to be at least minCapacity size.
// If minCapacity is <= capacity nothing happens,
// otherwise the list is expanded exponentially.
[pub] fun mut List<T>.grow(minCapacity int)
    require !isLocked()
    if minCapacity <= my.buffer.count
        my.incVersion()
        return
    @newCap = max(my.capacity*2, minCapacity)
    if sizeof(T) == 1
        newCap = max(newCap, 12)
    elif sizeof(T) <= 64
        newCap = max(newCap, 4)
    else
        newCap = max(newCap, 1)
    my.setCapacity(newCap)

[pub] fun mut List<T>.trim(extra int = 0)
    require !hasRef()        
    my.setCapacity(my.count + extra)

[pub] fun get List<T>.capacity() int
    return my.buffer.count

// Set the capacity, won't shrink below count.
[pub] fun mut List<T>.setCapacity(value int)
    require !isLocked()        
    @newCap = max(value, my.count)
    if newCap == my.buffer.count
        return
    require_tbd(newCap < my.MaxItems)
    
    // Copy items into new array
    @newBuffer = ListBuffer<T>(newCap)
    for @i in 0..my.count
        newBuffer[i] = move(my.buffer[i])
    my.buffer = newBuffer


// Return a new array with all occurences of `seqSearch`
// replaced by `seqReplace`.
[pub] fun mut List<T>.replace(seqSearch []T, seqReplace[]T)
    require !hasRef()
    todo

[pub] fun mut List<T>.clear()
    require !hasRef()
    todo

[pub] fun mut List<T>.push(item own T)
    require !hasRef()
    todo

[pub] fun mut List<T>.pushClones(items Span<T>)
    require !hasRef()
    todo

[pub] fun mut List<T>.pushClones(items mut Iterator<T>)
    require !hasRef()
    todo

[pub] fun mut List<T>.pushClones(item T, count int)
    require !hasRef()
    todo

[pub] fun mut List<T>.pop() ?T
    require !hasRef()
    todo

[pub] fun mut List<T>.insert(index int, item own T)
    require !hasRef()
    todo

[pub] fun mut List<T>.insert(index int, items []T)
    require !hasRef()
    todo

[pub] fun mut List<T>.insert(index int, items mut Iterator<T>)
    require !hasRef()
    todo

[pub] fun mut List<T>.remove(index int) T
    require !hasRef()
    todo

[pub] fun mut List<T>.remove(range Range<int>)
    require !hasRef()
    todo

[pub] fun mut List<T>.remove(test fun(item T) bool)
    require !hasRef()
    todo

[pub] fun List<T>.convert<Tout>(f fun(item T) ?Tout) List<Tout>
    where T has Clone<T>
    todo

[pub] fun List<T>.toArray() Array<T>
    where T has Clone<T>
    todo


// Usage: myList.For @item => DoStuffWithItem()
[pub] fun mut List<T>.forEach(f fun(item mut T)) {}

[pub] fun mut List<T>.getIter()
    return this[0..my.count]

 