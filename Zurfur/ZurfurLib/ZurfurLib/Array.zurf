module Zurfur

/// Immutable array
type pub ro Array<T> = Buffer<T>
{
    // NOTE: Much of the implmentation is inherited from Buffer
    //       and its implicit conversion to Span

    /// Return a new array with all occurences of `seqSearch`
    /// replaced by `seqReplace`.
    // TBD: Is it confusing to have this function be immutable,
    //      but List.Replace to be an in-place replacement
    fun pub Replace(seqSearch []T, seqReplace[]T) Array<T>:
        return todo()

}